version: "3.8"

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production

  frontend:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://backend:8080
